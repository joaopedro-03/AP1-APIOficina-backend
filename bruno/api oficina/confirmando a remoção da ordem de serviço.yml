info:
  name: confirmando a remoção da ordem de serviço
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/api/ordens/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
